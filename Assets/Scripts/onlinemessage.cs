using Mirror;

public class onlinemessage : MessageBase
{
    public string race;
    // Start is called before the first frame update
    void Start()
    {

        race = "the lazy fox jumped over the silly dogs";
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
